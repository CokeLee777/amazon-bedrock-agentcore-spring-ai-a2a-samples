server:
  port: 9000

spring:
  ai:
    bedrock:
      aws:
        region: ${BEDROCK_REGION:ap-northeast-2}
      converse:
        chat:
          options:
            model: ${BEDROCK_MODEL_ID:}
            max-tokens: ${BEDROCK_MAX_TOKENS:8096}
    a2a:
      server:
        enabled: true
        url: ${A2A_SERVER_URL:}
      remote:
        agents:
          delivery-agent:
            url: ${DELIVERY_AGENT_URL:}
          payment-agent:
            url: ${PAYMENT_AGENT_URL:}
    mcp:
      server:
        name: order-agent
        version: 1.0.0
        protocol: STREAMABLE
